<%= form_with(model: user, local: true) do |form| %>
  <% if user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(user.errors.count, "error") %> prohibited this user from being saved:</h2>

      <ul>
      <% user.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :loginId %>
    <%= form.text_field :loginId %>
  </div>

  <div class="field">
    <%= form.label :userName %>
    <%= form.text_field :userName %>
  </div>

  <div class="field">
    <%= form.label :password %>
    <%= form.text_field :password %>
  </div>

  <div class="field">
    <%= form.label :albumId %>
    <%= form.number_field :albumId %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
